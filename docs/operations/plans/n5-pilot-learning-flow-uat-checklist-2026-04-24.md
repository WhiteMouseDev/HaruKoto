# N5 파일럿 학습 플로우 UAT 체크리스트

> 작성일: 2026-04-24
> 대상: Mobile N5 Ch.01-Ch.06 파일럿
> 목적: 추천 경로와 자유 진입 정책이 실제 학습 행동에서 자연스럽게 이해되는지 검증한다.

## 1. 준비 조건

- 앱 빌드: 최신 모바일 브랜치 반영
- 대상 계정: 신규 N5 계정 1개, 일부 진행 계정 1개, 전부 완료 계정 1개
- 데이터: N5 Ch.01-Ch.06 seed 완료
- 기록 방식: 각 시나리오마다 `Pass`, `Flag`, `Block` 중 하나로 기록

## 2. 평가 기준

- `Pass`: 의도한 흐름이 설명 없이 이해되고 다음 행동이 자연스럽다.
- `Flag`: 진행은 가능하지만 문구, 강조, 피드백이 애매하다.
- `Block`: 학습 진입, 제출, 추천 갱신 중 하나가 실패하거나 오해를 유발한다.

## 3. 시나리오

### 시나리오 A. 신규 N5 유저 첫 진입

1. 학습 탭에 진입한다.
2. 추천 카드가 3초 안에 눈에 들어오는지 본다.
3. 추천 카드 라벨이 `추천 레슨`으로 보이는지 확인한다.
4. 카드 버튼 `시작하기`를 눌러 레슨 상세로 진입한다.
5. 상세 화면에서 바로 practice 진입이 가능한지 확인한다.

기록:

- 상태:
- 관찰:
- 후속 조치:

### 시나리오 B. 진행 중 레슨 이어하기

1. 한 레슨을 시작한 뒤 중간 단계에서 나간다.
2. 다시 학습 탭에 진입한다.
3. 추천 카드와 타일 라벨이 `이어하기`로 바뀌는지 확인한다.
4. 카드 버튼 `이어하기`를 눌렀을 때 해당 레슨으로 자연스럽게 복귀되는지 확인한다.

기록:

- 상태:
- 관찰:
- 후속 조치:

### 시나리오 C. 추천이 아닌 레슨 직접 진입

1. 추천 레슨이 아닌 다른 챕터 또는 레슨을 선택한다.
2. 경고 없이 상세 진입이 가능한지 확인한다.
3. 사용자가 잠금 구조라고 오해할 만한 표현이 없는지 본다.
4. 직접 진입 후 제출까지 완료한다.

기록:

- 상태:
- 관찰:
- 후속 조치:

### 시나리오 D. 추천 상태 갱신

1. 추천 레슨을 완료한다.
2. 목록으로 돌아와 다음 추천 레슨이 바뀌는지 확인한다.
3. 추천 배지가 새 챕터 또는 새 레슨에 맞게 이동하는지 본다.
4. 완료 레슨이 더 이상 추천 스타일로 남지 않는지 확인한다.

기록:

- 상태:
- 관찰:
- 후속 조치:

### 시나리오 E. 전부 완료 후 복습 진입

1. 파일럿 레슨을 모두 완료한 계정으로 학습 탭에 진입한다.
2. 추천 카드 라벨이 `다시 보기 추천`으로 보이는지 확인한다.
3. 추천 레슨 진입이 정상 동작하는지 확인한다.
4. 사용자가 새 학습이 아니라 복습 흐름임을 오해 없이 이해하는지 본다.

기록:

- 상태:
- 관찰:
- 후속 조치:

## 4. 공통 체크 질문

각 시나리오마다 아래 질문에 답한다.

1. `이어하기`와 `추천 레슨`의 차이를 설명 없이 이해할 수 있는가
2. `추천` 배지가 챕터 강조와 충돌하지 않는가
3. 추천 카드, 챕터 배지, 레슨 타일이 같은 대상을 가리키는 것이 명확한가
4. 자유 진입이 가능하다는 점이 충분히 드러나는가
5. 추천 경로를 따르지 않아도 완료, 제출, 진행률 반영이 정상인가

## 5. 계측 이벤트 확인

모바일 앱은 파일럿 UAT 중 아래 이벤트를 `LessonPilotTelemetry`로 남긴다.
초기 구현은 `debugPrint` sink이며, 테스트에서는 provider override로 이벤트를
캡처한다.

| 이벤트 | 확인 위치 | 핵심 속성 |
|--------|-----------|-----------|
| `lesson_list_viewed` | 학습 탭, 전체 레슨 화면 | `source`, `jlptLevel`, `chapterCount`, `lessonCount`, `recommendedLessonId` |
| `lesson_started` | 레슨 start API 성공 직후 | `lessonId`, `lessonNo`, `hasRecognitionStep`, `hasReorderStep` |
| `lesson_step_completed` | 레슨 단계 전환 | `lessonId`, `step`, `skipped` |
| `lesson_submitted` | submit API 성공/실패 | `lessonId`, `outcome`, `answerCount`, `status`, `errorType` |
| `lesson_completed` | submit 결과 `COMPLETED` | `lessonId`, `scoreCorrect`, `scoreTotal`, `srsItemsRegistered` |
| `lesson_retry_clicked` | 결과 화면 다시 풀기 | `lessonId` |
| `review_cta_clicked` | 복습 CTA 탭/버튼 | `jlptLevel`, `totalDue`, `wordDue`, `grammarDue`, `quizType` |

UAT 기록에는 시나리오별 `Pass/Flag/Block`과 함께 누락된 이벤트가 있는지
같이 남긴다. 이벤트가 빠지면 학습 행동 분석이 불가능하므로 `Flag`로 처리한다.

## 6. 합격 기준

- `Block` 없음
- 핵심 시나리오 A-D에서 `Pass` 1회 이상 확보
- 동일 문구에 대한 `Flag`가 2회 이상 반복되면 카피 수정 후 재검증
- 추천 갱신 오류가 1회라도 나오면 서버/클라이언트 상태 계산을 다시 점검
