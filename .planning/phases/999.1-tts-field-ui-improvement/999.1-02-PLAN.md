---
phase: 999.1-tts-field-ui-improvement
plan: 02
type: execute
wave: 2
depends_on: ["999.1-01"]
files_modified: []
autonomous: false
requirements:
  - BACKLOG-999.1
must_haves:
  truths:
    - "Vocabulary edit page shows 3 TTS field rows (reading, word, example sentence)"
    - "Grammar/quiz/conversation edit pages show 1 TTS field row each"
    - "Play button plays audio inline on the correct row"
    - "Generate/Regenerate button opens confirmation dialog and triggers TTS generation"
    - "No Select dropdown visible anywhere in the TTS section"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the TTS field list UI on all 4 content type edit pages.

Purpose: Confirm the refactored TtsPlayer renders correctly in production context — visual layout, play/pause interaction, and regeneration flow work as designed.

Output: User confirmation that the vertical field list UI works correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.1-tts-field-ui-improvement/999.1-CONTEXT.md
@.planning/phases/999.1-tts-field-ui-improvement/999.1-UI-SPEC.md
@.planning/phases/999.1-tts-field-ui-improvement/999.1-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify TTS field list UI on all 4 content type edit pages</name>
  <files></files>
  <action>
    Human verification checkpoint. TTS field list UI refactoring — Select dropdown replaced with vertical field list showing per-row status icons and action buttons across all 4 content type edit pages.

    Steps to verify:
    1. Start the dev server: `pnpm --filter admin dev`
    2. Navigate to a vocabulary item edit page (e.g., `/vocabulary/{any-id}`)
       - Verify: TTS section shows 3 rows: "Reading" / "Word" / "Example sentence"
       - Verify: Each row has a status icon (green check if audio exists, X if not)
       - Verify: No Select dropdown is visible
    3. Navigate to a grammar item edit page (e.g., `/grammar/{any-id}`)
       - Verify: TTS section shows 1 row: "Pattern"
    4. Navigate to a quiz item edit page (e.g., `/quiz/{any-id}`)
       - Verify: TTS section shows 1 row (either "Sentence" or "Japanese sentence")
    5. Navigate to a conversation item edit page (e.g., `/conversation/{any-id}`)
       - Verify: TTS section shows 1 row: "Situation"
    6. On any item with audio:
       - Click Play button on a row — audio plays, button changes to Pause
       - Click Pause — audio stops
       - (Vocabulary only) Click Play on a different row — previous row stops, new row plays
    7. Click RotateCcw (regenerate) button on a row with audio:
       - Verify: Confirmation dialog opens
       - Cancel — dialog closes, nothing happens
    8. Click Generate button on a row without audio:
       - Verify: Confirmation dialog opens
       - (Optional) Confirm to test actual generation if backend is available

    Resume signal: Type "approved" if UI works correctly, or describe issues found.
  </action>
  <verify>User confirms visual layout and interactions work correctly on all 4 content types</verify>
  <done>User approves the TTS field list UI with no regressions in playback or regeneration functionality.</done>
</task>

</tasks>

<verification>
User confirms all 4 content type edit pages display the correct TTS field list UI with working playback and regeneration interactions.
</verification>

<success_criteria>
- User approves the visual layout and interaction of the TTS field list on all 4 content types
- No regressions reported in existing functionality (playback, regeneration)
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-tts-field-ui-improvement/999.1-02-SUMMARY.md`
</output>
