---
phase: 999.4-table-system-improvement
plan: 03
subsystem: ui
tags: [next.js, table, sorting, url-state, react-query]

# Dependency graph
requires:
  - phase: 999.4-01
    provides: Backend returns sentence field + sort_by/sort_order query params on all 4 list endpoints
  - phase: 999.4-02
    provides: FilterBar controlled selects + ContentTable selection reset + keepPreviousData
provides:
  - Sortable column headers with URL-driven sort state across all 4 content tables
  - Quiz detail links include ?type=cloze|sentence_arrange query param
  - useContentList hook passes sort_by/sort_order from URL to API
affects: [999.4, future-phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "URL-driven sort state: sort_by/sort_order in searchParams, toggled via router.replace in ContentTable"
    - "sortKey on Column type enables opt-in sortable headers per column"

key-files:
  created: []
  modified:
    - apps/admin/src/hooks/use-content-list.ts
    - apps/admin/src/components/content/content-table.tsx
    - apps/admin/src/app/(admin)/quiz/page.tsx
    - apps/admin/src/app/(admin)/vocabulary/page.tsx
    - apps/admin/src/app/(admin)/grammar/page.tsx
    - apps/admin/src/app/(admin)/conversation/page.tsx

key-decisions:
  - "sortKey as optional Column property — opt-in sortable headers without breaking existing column definitions"
  - "ContentTable owns sort URL logic (router.replace) — single source of truth, not duplicated in each page"
  - "page reset to 1 on sort change — prevents phantom empty pages after sort switch"
  - "quiz detail link uses item.quizType === 'cloze' ? 'cloze' : 'sentence_arrange' — explicit mapping avoids ambiguity"

patterns-established:
  - "URL-driven sort: Column.sortKey -> ContentTable handleSort -> router.replace -> useContentList reads params -> API fetch"

requirements-completed: [BACKLOG-999.4]

# Metrics
duration: 5min
completed: 2026-03-30
---

# Phase 999.4 Plan 03: Table System Improvement (Sort + Quiz Link Fix) Summary

**Sortable column headers with URL-synced sort state on all 4 content tables, plus quiz detail links fixed to include quiz type param**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-30T04:04:00Z
- **Completed:** 2026-03-30T04:06:25Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Fixed quiz detail link to include `?type=cloze|sentence_arrange` so detail page loads correct form
- Added `sort_by` and `sort_order` URL param support to `useContentList` hook flowing to API
- Extended `Column` type with optional `sortKey` property for opt-in sortable headers
- ContentTable reads sort state from URL and handles toggle/direction via `router.replace`
- All 4 content tables (vocabulary, grammar, quiz, conversation) have sortable JLPT/Status/Date columns

## Task Commits

Each task was committed atomically:

1. **Task 1: Fix quiz detail link + add sort params to hook** - `6c6fa32` (feat)
2. **Task 2: Add sortable column headers + wire sort in all 4 pages** - `b9f9065` (feat)

**Plan metadata:** (final docs commit)

## Files Created/Modified
- `apps/admin/src/hooks/use-content-list.ts` - Added sort_by/sort_order URL param reading and passing to API
- `apps/admin/src/components/content/content-table.tsx` - Extended Column type with sortKey; added sort toggle logic with router.replace; sortable headers show ArrowUp/ArrowDown/ArrowUpDown icons
- `apps/admin/src/app/(admin)/quiz/page.tsx` - Fixed detail link to include ?type= param; added sortKey to jlptLevel/reviewStatus/updatedAt columns
- `apps/admin/src/app/(admin)/vocabulary/page.tsx` - Added sortKey to jlptLevel/reviewStatus/updatedAt columns
- `apps/admin/src/app/(admin)/grammar/page.tsx` - Added sortKey to jlptLevel/reviewStatus/updatedAt columns
- `apps/admin/src/app/(admin)/conversation/page.tsx` - Added sortKey to category/reviewStatus/updatedAt columns

## Decisions Made
- `sortKey` as optional `Column` property enables opt-in sortable headers without breaking existing column definitions
- `ContentTable` owns the sort URL logic (single `handleSort` function) rather than each page implementing it
- Sort change resets page to 1 to prevent phantom empty pages

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 999.4 complete — all 3 plans executed
- All 4 content tables have working sort, filter, and pagination
- Quiz table text column fixed (sentence field match from Plan 01)
- Quiz detail links work for both cloze and sentence_arrange types

---
*Phase: 999.4-table-system-improvement*
*Completed: 2026-03-30*
