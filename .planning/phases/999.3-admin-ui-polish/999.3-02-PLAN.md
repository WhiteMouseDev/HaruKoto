---
phase: 999.3-admin-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/components/features/dashboard/stats-card.tsx
autonomous: true
requirements: [BACKLOG-999.3]

must_haves:
  truths:
    - "Dashboard stats cards visually differentiate needs_review, approved, and rejected counts with distinct colors"
    - "Cards with pending review items have an amber left border accent drawing attention"
    - "Progress bar uses primary (cherry pink) color for fill"
  artifacts:
    - path: "apps/admin/src/components/features/dashboard/stats-card.tsx"
      provides: "Color-coded stats card with conditional left border"
      contains: "text-amber-500"
  key_links:
    - from: "apps/admin/src/components/features/dashboard/stats-card.tsx"
      to: "apps/admin/src/app/(admin)/dashboard/page.tsx"
      via: "StatsCard component import"
      pattern: "StatsCard"
---

<objective>
Redesign dashboard StatsCard with color-coded count rows and conditional amber left border for cards needing attention.

Purpose: Fix P4 — sparse dashboard cards with no visual hierarchy. Differentiate needs_review (amber), approved (green), and rejected (muted) counts.
Output: Production-quality stats cards with clear visual hierarchy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-admin-ui-polish/999.3-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Color-code StatsCard count rows and add conditional left border</name>
  <files>apps/admin/src/components/features/dashboard/stats-card.tsx</files>
  <read_first>
    - apps/admin/src/components/features/dashboard/stats-card.tsx
    - apps/admin/src/app/(admin)/dashboard/page.tsx
    - .planning/phases/999.3-admin-ui-polish/999.3-UI-SPEC.md
  </read_first>
  <action>
**1. Add `cn` import:**
```tsx
import { cn } from '@/lib/utils';
```

**2. Add conditional left border to the Card element:**
Replace `<Card>` with:
```tsx
<Card className={cn(needsReview > 0 && 'border-l-4 border-l-amber-400')}>
```

**3. Color-code the three count number spans:**

needs_review count (first span with `text-[28px]`):
```tsx
<span className="text-[28px] font-semibold leading-none text-amber-500">
  {needsReview}
</span>
```

approved count (second span with `text-[28px]`):
```tsx
<span className="text-[28px] font-semibold leading-none text-emerald-600">
  {approved}
</span>
```
Also add a total context label next to the approved label:
```tsx
<span className="text-sm text-muted-foreground">
  {t('approved')}
  <span className="ml-1 text-xs">/ {total}</span>
</span>
```

rejected count (third span with `text-[28px]`):
```tsx
<span className="text-[28px] font-semibold leading-none text-muted-foreground">
  {rejected}
</span>
```

**4. Keep everything else unchanged:** CardHeader icon+title layout, progress bar with `bg-primary`, progress label, space-y-3 content spacing. The `StatsCardProps` type is unchanged.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese && grep -q "text-amber-500" apps/admin/src/components/features/dashboard/stats-card.tsx && grep -q "text-emerald-600" apps/admin/src/components/features/dashboard/stats-card.tsx && grep -q "border-l-amber-400" apps/admin/src/components/features/dashboard/stats-card.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - stats-card.tsx contains `text-amber-500` for needs_review count
    - stats-card.tsx contains `text-emerald-600` for approved count
    - stats-card.tsx contains `text-muted-foreground` on the rejected count span (the one with `text-[28px]`)
    - stats-card.tsx contains `border-l-4 border-l-amber-400` conditional class on Card
    - stats-card.tsx imports `cn` from `@/lib/utils`
    - stats-card.tsx contains `/ {total}` for the approved row context label
    - StatsCardProps type is unchanged (same 6 props: title, icon, needsReview, approved, rejected, total)
  </acceptance_criteria>
  <done>Dashboard stats cards show color-coded counts (amber needs_review, green approved, muted rejected) with amber left border on cards with pending items</done>
</task>

</tasks>

<verification>
- `pnpm --filter admin build` completes without errors
- Stats card has three distinct color classes for the three count numbers
- Conditional border-l-4 applied only when needsReview > 0
</verification>

<success_criteria>
- needs_review count renders in amber (text-amber-500)
- approved count renders in green (text-emerald-600)
- rejected count renders in muted gray (text-muted-foreground)
- Cards with needsReview > 0 have border-l-4 border-l-amber-400 left accent
- Total context shown as "/ {total}" next to approved label
- Admin app builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-admin-ui-polish/999.3-02-SUMMARY.md`
</output>
