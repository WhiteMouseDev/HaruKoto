---
phase: 999.3-admin-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/app/(admin)/layout.tsx
  - apps/admin/src/components/layout/sidebar.tsx
  - apps/admin/src/components/layout/sidebar-badge.tsx
autonomous: true
requirements: [BACKLOG-999.3]

must_haves:
  truths:
    - "Logo and app name appear only once in the UI (sidebar), not duplicated in a header"
    - "LocaleSwitcher and LogoutButton appear only in the sidebar bottom, not in a header"
    - "User display name is visible in sidebar bottom zone"
    - "Sidebar needs_review badge uses informational pink, not alarm-red destructive color"
  artifacts:
    - path: "apps/admin/src/app/(admin)/layout.tsx"
      provides: "Simplified layout without Header component"
      contains: "Sidebar user={user}"
    - path: "apps/admin/src/components/layout/sidebar.tsx"
      provides: "Sidebar with user display name in bottom zone"
      contains: "displayName"
    - path: "apps/admin/src/components/layout/sidebar-badge.tsx"
      provides: "Informational pink badge"
      contains: "bg-primary/15 text-primary"
  key_links:
    - from: "apps/admin/src/app/(admin)/layout.tsx"
      to: "apps/admin/src/components/layout/sidebar.tsx"
      via: "user prop passed from requireReviewer()"
      pattern: "Sidebar.*user="
---

<objective>
Remove the duplicated Header component, consolidate identity and controls into the sidebar, and fix the badge color from alarm-red to informational pink.

Purpose: Eliminate visual duplication (P1 + P2) and fix misleading badge color (P3) per the UI-SPEC contract.
Output: Cleaned layout with sidebar as the sole chrome, pink informational badges.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.3-admin-ui-polish/999.3-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Header, restructure layout, add user prop to Sidebar</name>
  <files>apps/admin/src/app/(admin)/layout.tsx, apps/admin/src/components/layout/sidebar.tsx</files>
  <read_first>
    - apps/admin/src/app/(admin)/layout.tsx
    - apps/admin/src/components/layout/sidebar.tsx
    - apps/admin/src/components/layout/header.tsx
    - .planning/phases/999.3-admin-ui-polish/999.3-UI-SPEC.md
  </read_first>
  <action>
**layout.tsx changes:**
1. Remove the `import { Header } from '@/components/layout/header';` line entirely
2. Remove the `<Header user={user} locale={locale} />` JSX line
3. Pass `user` and `locale` props to Sidebar: `<Sidebar user={user} locale={locale} />`
4. Simplify the outer wrapper from nested `flex flex-col` + `flex flex-1` to a single `flex min-h-screen`:

```tsx
return (
  <div className="flex min-h-screen">
    <Sidebar user={user} locale={locale} />
    <main className="flex-1 overflow-y-auto p-8">{children}</main>
  </div>
);
```

**sidebar.tsx changes:**
1. Add `import type { User } from '@supabase/supabase-js';` at the top
2. Change function signature from `Sidebar()` to `Sidebar({ user, locale }: { user: User; locale: string })`
3. Remove the internal `const locale = await getLocale();` call (locale now comes from props)
4. Compute displayName inside the component:
```tsx
const displayName =
  (user.user_metadata?.full_name as string | undefined) ??
  user.email?.split('@')[0] ??
  'Reviewer';
```
5. Update the sidebar bottom zone to add displayName above LocaleSwitcher. Pass `currentLocale={locale}` (already done). Final bottom zone markup:
```tsx
<div className="flex flex-col gap-2 border-t border-border px-4 py-4">
  <span className="text-xs text-muted-foreground">{displayName}</span>
  <LocaleSwitcher currentLocale={locale} />
  <LogoutButton label={tAuth('logout')} />
</div>
```
6. Remove the `getLocale` import from `next-intl/server` (keep `getTranslations`)

Do NOT delete header.tsx file yet — just remove its usage. The file can be cleaned up later.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese && grep -c "Header" apps/admin/src/app/\(admin\)/layout.tsx | grep -q "^0$" && echo "PASS: No Header references in layout" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx contains NO import of Header: `grep "Header" apps/admin/src/app/(admin)/layout.tsx` returns 0 lines
    - layout.tsx contains `<Sidebar user={user} locale={locale}`: grep confirms
    - layout.tsx outer div has class `flex min-h-screen` (no `flex-col`)
    - sidebar.tsx function signature includes `user: User`
    - sidebar.tsx contains `text-xs text-muted-foreground` span with `{displayName}`
    - sidebar.tsx does NOT import `getLocale` from `next-intl/server`
  </acceptance_criteria>
  <done>Header removed from layout, user display name visible in sidebar bottom, layout simplified to sidebar+main only</done>
</task>

<task type="auto">
  <name>Task 2: Fix NavBadge color from destructive red to informational pink</name>
  <files>apps/admin/src/components/layout/sidebar-badge.tsx</files>
  <read_first>
    - apps/admin/src/components/layout/sidebar-badge.tsx
    - .planning/phases/999.3-admin-ui-polish/999.3-UI-SPEC.md
  </read_first>
  <action>
In `sidebar-badge.tsx`, replace the className string on the `<span>` element.

**Current classes (line 6):**
```
ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-destructive px-1 text-[10px] font-semibold text-destructive-foreground
```

**New classes:**
```
ml-auto flex h-5 min-w-5 items-center justify-center rounded-full bg-primary/15 px-1 text-[10px] font-semibold text-primary
```

Changes: `bg-destructive` -> `bg-primary/15`, `text-destructive-foreground` -> `text-primary`.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese && grep -q "bg-primary/15" apps/admin/src/components/layout/sidebar-badge.tsx && grep -q "text-primary" apps/admin/src/components/layout/sidebar-badge.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sidebar-badge.tsx contains `bg-primary/15` (not `bg-destructive`)
    - sidebar-badge.tsx contains `text-primary` (not `text-destructive-foreground`)
    - sidebar-badge.tsx does NOT contain the string `destructive`
  </acceptance_criteria>
  <done>NavBadge renders with informational cherry-pink background instead of alarm-red</done>
</task>

</tasks>

<verification>
- `pnpm --filter admin build` completes without errors
- No references to `Header` component in layout.tsx
- No references to `destructive` in sidebar-badge.tsx
- Sidebar renders user display name, locale switcher, and logout button
</verification>

<success_criteria>
- Header component is no longer rendered in the admin layout
- User display name appears in sidebar bottom zone
- NavBadge uses bg-primary/15 text-primary (informational pink)
- Admin app builds without TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/phases/999.3-admin-ui-polish/999.3-01-SUMMARY.md`
</output>
