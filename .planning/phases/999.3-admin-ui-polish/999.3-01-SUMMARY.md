---
phase: 999.3-admin-ui-polish
plan: "01"
subsystem: admin-layout
tags: [layout, sidebar, ui-polish, header-removal, badge-color]
dependency_graph:
  requires: []
  provides: [simplified-admin-layout, sidebar-with-user, informational-badge]
  affects: [apps/admin/src/app/(admin)/layout.tsx, apps/admin/src/components/layout/sidebar.tsx, apps/admin/src/components/layout/sidebar-badge.tsx]
tech_stack:
  added: []
  patterns: [user-prop-drilling-from-layout, displayName-in-sidebar]
key_files:
  created: []
  modified:
    - apps/admin/src/app/(admin)/layout.tsx
    - apps/admin/src/components/layout/sidebar.tsx
    - apps/admin/src/components/layout/sidebar-badge.tsx
decisions:
  - Header component removed from AdminLayout; sidebar is now the sole chrome
  - user prop passed from requireReviewer() in layout down to Sidebar
  - displayName computed in Sidebar from user_metadata.full_name or email prefix
  - NavBadge color changed from bg-destructive to bg-primary/15 text-primary
metrics:
  duration: 5m
  completed_date: "2026-03-30"
  tasks_completed: 2
  files_modified: 3
---

# Phase 999.3 Plan 01: Header Removal and Badge Color Fix Summary

**One-liner:** Removed duplicated Header component from AdminLayout and changed NavBadge from alarm-red destructive to informational cherry-pink bg-primary/15.

## What Was Done

### Task 1: Remove Header, restructure layout, add user prop to Sidebar

- Removed `import { Header }` and `<Header user={user} locale={locale} />` from `apps/admin/src/app/(admin)/layout.tsx`
- Simplified layout wrapper from nested `flex flex-col` + inner `flex flex-1` to a single `flex min-h-screen`
- Pass `user` and `locale` props from AdminLayout to `<Sidebar user={user} locale={locale} />`
- Updated `sidebar.tsx` function signature to accept `{ user: User; locale: string }`
- Added `import type { User } from '@supabase/supabase-js'`
- Removed `getLocale` from `next-intl/server` import in sidebar (locale now received via prop)
- Computed `displayName` in Sidebar from `user.user_metadata?.full_name` or email prefix or `'Reviewer'` fallback
- Added `<span className="text-xs text-muted-foreground">{displayName}</span>` in sidebar bottom zone above LocaleSwitcher

### Task 2: Fix NavBadge color from destructive red to informational pink

- In `apps/admin/src/components/layout/sidebar-badge.tsx`, replaced:
  - `bg-destructive` with `bg-primary/15`
  - `text-destructive-foreground` with `text-primary`
- Badge now communicates "items pending your attention" (informational) rather than "error/alarm" state

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | 7817b44 | feat(999.3-01): remove Header, pass user prop to Sidebar with displayName in bottom zone |
| Task 2 | f50d797 | fix(999.3-01): change NavBadge from alarm-red to informational cherry-pink |

## Verification

- `pnpm --filter admin build` — PASSED (compiled successfully, 10 static pages generated)
- No Header references in layout.tsx: confirmed (grep returns 0)
- Sidebar receives `user={user} locale={locale}`: confirmed
- sidebar-badge.tsx contains `bg-primary/15` and `text-primary`, no `destructive`: confirmed

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — all changes are complete UI fixes, no placeholder data.

## Self-Check: PASSED

- layout.tsx exists: FOUND
- sidebar.tsx exists: FOUND
- sidebar-badge.tsx exists: FOUND
- Commit 7817b44 exists: FOUND
- Commit f50d797 exists: FOUND
