---
phase: 08-i18n-gap-closure-tts-hook-toast
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/hooks/use-tts-player.ts
  - apps/admin/src/__tests__/hardcoded-strings.test.ts
autonomous: true
requirements:
  - I18N-04
  - I18N-05

must_haves:
  truths:
    - "TTS regenerate success toast displays locale-aware text (ko/ja/en) instead of hardcoded Japanese"
    - "TTS regenerate error toast displays locale-aware text instead of hardcoded Japanese or raw server error"
    - "hardcoded-strings.test.ts scans both .ts and .tsx files and passes with zero violations"
  artifacts:
    - path: "apps/admin/src/hooks/use-tts-player.ts"
      provides: "i18n-aware TTS toast messages"
      contains: "useTranslations"
    - path: "apps/admin/src/__tests__/hardcoded-strings.test.ts"
      provides: "Extended CJK scan covering .ts files"
      contains: "findSourceFiles"
  key_links:
    - from: "apps/admin/src/hooks/use-tts-player.ts"
      to: "apps/admin/messages/ja.json"
      via: "useTranslations('tts') resolving regenerateSuccess/regenerateError keys"
      pattern: "t\\('regenerateSuccess'\\)|t\\('regenerateError'\\)"
    - from: "apps/admin/src/__tests__/hardcoded-strings.test.ts"
      to: "apps/admin/src/hooks/use-tts-player.ts"
      via: "file scan now includes .ts files"
      pattern: "entry\\.name\\.endsWith\\('\\.ts'\\)"
---

<objective>
Replace hardcoded Japanese toast messages in useTtsPlayer hook with i18n-aware translations and extend the hardcoded-strings test to scan .ts files alongside .tsx.

Purpose: Close the 2 i18n gaps found in v1.1 milestone audit — useTtsPlayer is the last source of hardcoded CJK strings, and the test was blind to .ts files.
Output: Modified use-tts-player.ts (i18n toasts) + modified hardcoded-strings.test.ts (.ts scan)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-i18n-gap-closure-tts-hook-toast/08-CONTEXT.md
@.planning/phases/08-i18n-gap-closure-tts-hook-toast/08-RESEARCH.md

@apps/admin/src/hooks/use-tts-player.ts
@apps/admin/src/__tests__/hardcoded-strings.test.ts

<interfaces>
<!-- Existing i18n keys in locale files (tts namespace) — NO locale file changes needed -->

From apps/admin/messages/ko.json (tts section):
  "regenerateSuccess": "TTS를 재생성했습니다"
  "regenerateError": "재생성에 실패했습니다. 다시 시도해 주세요."

From apps/admin/messages/ja.json (tts section):
  "regenerateSuccess": "TTSを再生成しました"
  "regenerateError": "再生成に失敗しました。もう一度お試しください。"

From apps/admin/messages/en.json (tts section):
  "regenerateSuccess": "TTS regenerated"
  "regenerateError": "Regeneration failed. Please try again."

<!-- next-intl useTranslations API -->
import { useTranslations } from 'next-intl';
const t = useTranslations('tts');
t('regenerateSuccess') // returns locale-appropriate string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded Japanese toast with useTranslations in useTtsPlayer</name>
  <files>apps/admin/src/hooks/use-tts-player.ts</files>
  <read_first>
    - apps/admin/src/hooks/use-tts-player.ts (current hardcoded strings at lines 78, 81)
    - apps/admin/messages/ja.json (confirm tts.regenerateSuccess and tts.regenerateError keys exist)
  </read_first>
  <action>
Modify `apps/admin/src/hooks/use-tts-player.ts`:

1. Add import at the top (after existing imports):
   ```
   import { useTranslations } from 'next-intl';
   ```

2. Add `const t = useTranslations('tts');` as the first line inside `useTtsPlayer` function body (line 15, after the opening brace), before `const queryClient = useQueryClient();`.

3. Replace line 78 — change:
   ```
   toast.success('TTSを再生成しました');
   ```
   to:
   ```
   toast.success(t('regenerateSuccess'));
   ```
   (per D-01, D-02 — useTranslations('tts') in hook, t() pattern for toast)

4. Replace line 80-81 — change the entire onError callback from:
   ```
   onError: (err: Error) => {
     toast.error(err.message || '再生成に失敗しました。もう一度お試しください。');
     setConfirmField(null);
   },
   ```
   to:
   ```
   onError: () => {
     toast.error(t('regenerateError'));
     setConfirmField(null);
   },
   ```
   Note: Remove `err: Error` parameter entirely since err.message is no longer used (per D-04 — always use i18n message, ignore server error message).

Key naming decision (Claude's Discretion): Use existing flat keys `regenerateSuccess` / `regenerateError` instead of adding nested `regenerate.success` / `regenerate.error`. The flat keys already exist in all 3 locale files with correct translations. This avoids key duplication. No locale file changes needed.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese/apps/admin && pnpm vitest run src/__tests__/hardcoded-strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `use-tts-player.ts` contains `import { useTranslations } from 'next-intl';`
    - `use-tts-player.ts` contains `const t = useTranslations('tts');`
    - `use-tts-player.ts` contains `toast.success(t('regenerateSuccess'))`
    - `use-tts-player.ts` contains `toast.error(t('regenerateError'))`
    - `use-tts-player.ts` does NOT contain the string `TTSを再生成しました`
    - `use-tts-player.ts` does NOT contain the string `再生成に失敗しました`
    - `use-tts-player.ts` onError callback does NOT reference `err.message` or `err: Error`
  </acceptance_criteria>
  <done>useTtsPlayer hook uses useTranslations('tts') for all toast messages; no hardcoded CJK strings remain in the file</done>
</task>

<task type="auto">
  <name>Task 2: Extend hardcoded-strings test to scan .ts files</name>
  <files>apps/admin/src/__tests__/hardcoded-strings.test.ts</files>
  <read_first>
    - apps/admin/src/__tests__/hardcoded-strings.test.ts (current findTsxFiles function and test description)
  </read_first>
  <action>
Modify `apps/admin/src/__tests__/hardcoded-strings.test.ts`:

1. Rename function `findTsxFiles` to `findSourceFiles` (lines 11 and 20):
   - Line 11: `function findTsxFiles(dir: string): string[]` -> `function findSourceFiles(dir: string): string[]`
   - Line 20 (recursive call): `results.push(...findTsxFiles(fullPath));` -> `results.push(...findSourceFiles(fullPath));`

2. Extend file filter at line 21 — change:
   ```
   } else if (entry.isFile() && entry.name.endsWith('.tsx')) {
   ```
   to:
   ```
   } else if (entry.isFile() && (entry.name.endsWith('.tsx') || entry.name.endsWith('.ts'))) {
   ```
   (per D-05 — scan both .ts and .tsx)

3. Update test description at line 29 — change:
   ```
   it('no .tsx source files contain CJK characters outside allowlist', () => {
   ```
   to:
   ```
   it('no .ts/.tsx source files contain CJK characters outside allowlist', () => {
   ```

4. Update function call at line 31 — change:
   ```
   const files = findTsxFiles(srcDir);
   ```
   to:
   ```
   const files = findSourceFiles(srcDir);
   ```

5. Existing exclusions remain unchanged (per D-06, D-07):
   - `__tests__/` directory excluded (line 18)
   - `node_modules/` directory excluded (line 17)
   - ALLOWLIST contains only `locale-switcher.tsx` (line 6)
   - Comment lines starting with `//` are skipped (line 47)
   - Import lines are skipped (line 46)
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese/apps/admin && pnpm vitest run src/__tests__/hardcoded-strings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `hardcoded-strings.test.ts` contains `function findSourceFiles(dir: string)` (NOT `findTsxFiles`)
    - `hardcoded-strings.test.ts` contains `entry.name.endsWith('.ts')` (the .ts extension check)
    - `hardcoded-strings.test.ts` contains `entry.name.endsWith('.tsx')` (kept from original)
    - `hardcoded-strings.test.ts` test description contains `no .ts/.tsx source files`
    - `hardcoded-strings.test.ts` contains `findSourceFiles(fullPath)` (recursive call renamed)
    - `hardcoded-strings.test.ts` contains `findSourceFiles(srcDir)` (invocation renamed)
    - Test passes with zero violations: `pnpm vitest run src/__tests__/hardcoded-strings.test.ts` exits 0
  </acceptance_criteria>
  <done>hardcoded-strings test scans both .ts and .tsx files, function renamed to findSourceFiles, test passes green</done>
</task>

</tasks>

<verification>
1. `cd apps/admin && pnpm vitest run src/__tests__/hardcoded-strings.test.ts` — passes with zero CJK violations (covers both Task 1 fix and Task 2 scan extension)
2. `cd apps/admin && pnpm vitest run` — full test suite passes
3. `grep -n 'TTSを再生成しました\|再生成に失敗しました' apps/admin/src/hooks/use-tts-player.ts` — returns zero results
4. `grep -n "useTranslations" apps/admin/src/hooks/use-tts-player.ts` — returns the import and usage lines
</verification>

<success_criteria>
- useTtsPlayer hook contains zero hardcoded CJK strings
- Toast messages use useTranslations('tts') with existing regenerateSuccess/regenerateError keys
- hardcoded-strings.test.ts scans .ts + .tsx files and passes
- Full vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-i18n-gap-closure-tts-hook-toast/08-01-SUMMARY.md`
</output>
