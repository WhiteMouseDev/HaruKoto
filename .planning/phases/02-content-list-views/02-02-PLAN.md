---
phase: 02-content-list-views
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/components/ui/table.tsx
  - apps/admin/src/components/layout/sidebar.tsx
  - apps/admin/src/components/layout/sidebar-nav-item.tsx
  - apps/admin/src/app/(admin)/layout.tsx
  - apps/admin/src/components/ui/status-badge.tsx
  - apps/admin/src/components/ui/pagination-bar.tsx
  - apps/admin/src/components/content/filter-bar.tsx
  - apps/admin/messages/ja.json
  - apps/admin/messages/ko.json
  - apps/admin/messages/en.json
  - apps/admin/src/__tests__/status-badge.test.tsx
autonomous: true
requirements:
  - LIST-07
  - LIST-05
  - LIST-06

must_haves:
  truths:
    - "Sidebar displays 5 navigation items with correct icons and highlights active route"
    - "Admin layout is two-column: sidebar (240px) + main content"
    - "StatusBadge renders 3 visual states with correct colors"
    - "FilterBar has search input, JLPT dropdown, category dropdown, status dropdown"
    - "PaginationBar renders numbered page buttons with active page highlighted"
    - "All new UI text has i18n keys in ja/ko/en"
  artifacts:
    - path: "apps/admin/src/components/layout/sidebar.tsx"
      provides: "240px left sidebar with 5 nav items"
      min_lines: 30
    - path: "apps/admin/src/components/ui/status-badge.tsx"
      provides: "needs_review/approved/rejected badge"
      contains: "bg-amber-100"
    - path: "apps/admin/src/components/ui/pagination-bar.tsx"
      provides: "Numbered pagination controls"
      contains: "page"
    - path: "apps/admin/src/components/content/filter-bar.tsx"
      provides: "Search + 3 filter dropdowns"
      contains: "useSearchParams"
  key_links:
    - from: "apps/admin/src/app/(admin)/layout.tsx"
      to: "apps/admin/src/components/layout/sidebar.tsx"
      via: "import and render in flex layout"
      pattern: "Sidebar"
    - from: "apps/admin/src/components/layout/sidebar-nav-item.tsx"
      to: "next/navigation usePathname"
      via: "active route detection"
      pattern: "usePathname"
---

<objective>
Build the admin UI shell: sidebar navigation, shared content list components (table, filter bar, pagination, status badge), and i18n keys.

Purpose: Create all reusable UI components needed for content list pages. These components are consumed by Plan 03 which wires them to the API data.

Output: Sidebar layout, StatusBadge, FilterBar, PaginationBar components; shadcn Table installed; i18n keys for all Phase 2 copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-list-views/02-CONTEXT.md
@.planning/phases/02-content-list-views/02-UI-SPEC.md
@.planning/phases/02-content-list-views/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts from Phase 1 admin app. -->

From apps/admin/src/app/(admin)/layout.tsx:
```tsx
export default async function AdminLayout({ children }: { children: React.ReactNode }) {
  const user = await requireReviewer();
  const locale = await getLocale();
  return (
    <div className="flex min-h-screen flex-col">
      <Header user={user} locale={locale} />
      <main className="flex-1 overflow-y-auto p-8">{children}</main>
    </div>
  );
}
```

From apps/admin/src/components/layout/header.tsx:
```tsx
type HeaderProps = { user: User; locale: string };
export async function Header({ user, locale }: HeaderProps) { ... }
// h-14, border-b border-border, bg-card, px-6, flex items-center justify-between
```

From apps/admin/src/lib/utils.ts:
```tsx
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

From apps/admin/messages/ja.json:
```json
{
  "auth": { ... },
  "dashboard": { "welcome": "...", "vocabulary": "単語", "grammar": "文法", "quiz": "クイズ", "conversation": "会話" },
  "locale": { ... },
  "common": { "appName": "HaruKoto Admin" }
}
```

Installed shadcn components: button, card, input, label, form, separator, dropdown-menu, sonner
Missing: table (must run `pnpm dlx shadcn@latest add table`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Table + Sidebar + layout modification + i18n keys</name>
  <files>
    apps/admin/src/components/ui/table.tsx,
    apps/admin/src/components/layout/sidebar.tsx,
    apps/admin/src/components/layout/sidebar-nav-item.tsx,
    apps/admin/src/app/(admin)/layout.tsx,
    apps/admin/messages/ja.json,
    apps/admin/messages/ko.json,
    apps/admin/messages/en.json
  </files>
  <read_first>
    apps/admin/src/app/(admin)/layout.tsx,
    apps/admin/src/components/layout/header.tsx,
    apps/admin/src/components/layout/locale-switcher.tsx,
    apps/admin/src/components/layout/logout-button.tsx,
    apps/admin/messages/ja.json,
    apps/admin/messages/ko.json,
    apps/admin/messages/en.json,
    apps/admin/components.json,
    .planning/phases/02-content-list-views/02-UI-SPEC.md
  </read_first>
  <action>
    1. Install shadcn Table component:
       ```bash
       cd apps/admin && pnpm dlx shadcn@latest add table
       ```

    2. Create `apps/admin/src/components/layout/sidebar-nav-item.tsx` ('use client' component):
       - Uses `usePathname()` from `next/navigation` to detect active route
       - Props: `{ href: string; icon: React.ReactNode; label: string }`
       - Renders `<Link>` with these styles per UI-SPEC:
         - Active: `border-l-2 border-primary bg-accent text-foreground`
         - Inactive: `text-muted-foreground hover:text-foreground hover:bg-accent/50`
       - Height: 40px (`h-10`), horizontal padding 16px (`px-4`), icon+label gap 8px (`gap-2`)
       - Active detection: `pathname === href` or `pathname.startsWith(href + '/')` (but not for `/dashboard` which is exact match)

    3. Create `apps/admin/src/components/layout/sidebar.tsx` (Server Component):
       - Fixed width 240px (`w-60`), `bg-background border-r border-border`
       - Full height flex column: `flex flex-col h-full`
       - Top section: Logo + app name (same as header logo, reuse Image import)
       - Navigation section: 5 items using `<SidebarNavItem>` with lucide icons:
         - LayoutDashboard -> /dashboard
         - BookOpen -> /vocabulary
         - BookMarked -> /grammar
         - HelpCircle -> /quiz
         - MessageSquare -> /conversation
       - Use `getTranslations()` for nav labels from i18n keys: `nav.dashboard`, `nav.vocabulary`, `nav.grammar`, `nav.quiz`, `nav.conversation`
       - Bottom section (mt-auto): LocaleSwitcher + LogoutButton (move from header to sidebar bottom per UI-SPEC sidebar spec)

    4. Modify `apps/admin/src/app/(admin)/layout.tsx`:
       - Change layout structure from single column to sidebar + main:
       ```tsx
       return (
         <div className="flex min-h-screen flex-col">
           <Header user={user} locale={locale} />
           <div className="flex flex-1 overflow-hidden">
             <Sidebar />
             <main className="flex-1 overflow-y-auto p-8">{children}</main>
           </div>
         </div>
       );
       ```
       - Import Sidebar from `@/components/layout/sidebar`
       - Header still spans full width at top

    5. Update all 3 i18n message files (ja.json, ko.json, en.json) with ALL Phase 2 strings from UI-SPEC Copywriting Contract. Add these top-level keys:
       - `nav`: sidebar navigation labels
       - `filter`: search placeholder, JLPT label, category label, status label, "all" options
       - `status`: needs_review, approved, rejected display names
       - `table`: detail link, pagination prev/next, items per page, total items
       - `empty`: no data heading, no results body, no content body
       - `error`: failed to load, network error
       - Update `dashboard` section: add welcome, progress label, stats labels

       ja.json nav section example:
       ```json
       "nav": {
         "dashboard": "ダッシュボード",
         "vocabulary": "単語",
         "grammar": "文法",
         "quiz": "クイズ",
         "conversation": "会話"
       }
       ```
       See UI-SPEC Copywriting Contract for ALL exact strings in ja/ko/en.
  </action>
  <verify>
    <automated>cd apps/admin && test -f src/components/ui/table.tsx && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/admin/src/components/ui/table.tsx` succeeds (shadcn Table installed)
    - `grep "usePathname" apps/admin/src/components/layout/sidebar-nav-item.tsx` finds active route detection
    - `grep "'use client'" apps/admin/src/components/layout/sidebar-nav-item.tsx` confirms client component
    - `grep "w-60" apps/admin/src/components/layout/sidebar.tsx` confirms 240px width
    - `grep "LayoutDashboard" apps/admin/src/components/layout/sidebar.tsx` confirms lucide icon import
    - `grep "Sidebar" apps/admin/src/app/(admin)/layout.tsx` confirms sidebar is rendered
    - `grep '"nav"' apps/admin/messages/ja.json` confirms nav i18n keys exist
    - `grep '"filter"' apps/admin/messages/ja.json` confirms filter i18n keys exist
    - `grep '"status"' apps/admin/messages/ja.json` confirms status i18n keys exist
    - `grep "検索" apps/admin/messages/ja.json` finds search placeholder
    - `grep "검색" apps/admin/messages/ko.json` finds Korean search placeholder
    - `grep "Search" apps/admin/messages/en.json` finds English search placeholder
  </acceptance_criteria>
  <done>Sidebar rendered in admin layout with 5 nav items, shadcn Table installed, all Phase 2 i18n keys in ja/ko/en</done>
</task>

<task type="auto">
  <name>Task 2: StatusBadge + FilterBar + PaginationBar + test</name>
  <files>
    apps/admin/src/components/ui/status-badge.tsx,
    apps/admin/src/components/content/filter-bar.tsx,
    apps/admin/src/components/ui/pagination-bar.tsx,
    apps/admin/src/__tests__/status-badge.test.tsx
  </files>
  <read_first>
    apps/admin/src/lib/utils.ts,
    apps/admin/src/components/ui/button.tsx,
    apps/admin/src/components/ui/input.tsx,
    apps/admin/src/components/ui/dropdown-menu.tsx,
    apps/admin/vitest.config.ts,
    apps/admin/src/__tests__/setup.ts,
    .planning/phases/02-content-list-views/02-UI-SPEC.md
  </read_first>
  <action>
    Per D-08, D-10, D-11, D-12, D-07:

    1. Create `apps/admin/src/components/ui/status-badge.tsx`:
       - Props: `{ status: 'needs_review' | 'approved' | 'rejected' }`
       - Renders `<span>` with `inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium`
       - Color mapping (per UI-SPEC Status Badge Colors):
         - needs_review: `bg-amber-100 text-amber-700 dark:bg-amber-900/30 dark:text-amber-400`
         - approved: `bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400`
         - rejected: `bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400`
       - Uses `useTranslations('status')` for display text: t('needsReview'), t('approved'), t('rejected')
       - 'use client' directive (uses useTranslations)

    2. Create `apps/admin/src/components/content/filter-bar.tsx` ('use client'):
       - Uses `useSearchParams()`, `useRouter()`, `usePathname()` from `next/navigation`
       - Uses `useTranslations('filter')` for labels
       - Props: `{ showCategory?: boolean; categories?: { value: string; label: string }[] }`
       - Renders single horizontal row (`flex items-center gap-4`):
         - Search input: `flex-1 max-w-xs` (max-width 320px), 40px height, placeholder from i18n `filter.searchPlaceholder`
         - 300ms debounce on search input (use React 19 `useDeferredValue` or manual setTimeout with cleanup)
         - JLPT level select: `w-[120px]`, options N5-N1 + "all" option from i18n
         - Category select: `w-[160px]`, only rendered if `showCategory` is true
         - Status select: `w-[144px]`, options needs_review/approved/rejected + "all"
       - Use native `<select>` styled with shadcn Input classes, OR shadcn DropdownMenu. Choose `<select>` for simplicity (no click-outside handling needed).
       - URL update: on any filter change, call `router.replace(pathname + '?' + params.toString())`. When any non-page filter changes, reset page to 1.
       - Wrap in `<Suspense>` at import site (note: the page.tsx that uses FilterBar must wrap it in Suspense)

    3. Create `apps/admin/src/components/ui/pagination-bar.tsx` ('use client'):
       - Props: `{ currentPage: number; totalPages: number }`
       - Uses `useSearchParams()`, `useRouter()`, `usePathname()`
       - Page number display: first, last, current, 2 neighbors, ellipsis for gaps
       - Active page: `bg-primary text-primary-foreground` (cherry pink)
       - Inactive: `bg-card text-foreground hover:bg-muted/50 border border-border rounded-md`
       - Min-width 32px per button, Label size text
       - Prev/Next buttons with ChevronLeft/ChevronRight icons from lucide
       - On page click: update `page` URL param via `router.replace()`

    4. Create `apps/admin/src/__tests__/status-badge.test.tsx`:
       - Test that StatusBadge renders with correct CSS classes for each status
       - Test needs_review renders with `bg-amber-100` class
       - Test approved renders with `bg-green-100` class
       - Test rejected renders with `bg-red-100` class
       - Use `@testing-library/react` render + screen.getByText
       - Mock `next-intl` useTranslations to return status labels
       - This satisfies VALIDATION.md Wave 0 for LIST-07
  </action>
  <verify>
    <automated>cd apps/admin && pnpm lint && pnpm test -- --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - `grep "bg-amber-100" apps/admin/src/components/ui/status-badge.tsx` confirms needs_review color
    - `grep "bg-green-100" apps/admin/src/components/ui/status-badge.tsx` confirms approved color
    - `grep "bg-red-100" apps/admin/src/components/ui/status-badge.tsx` confirms rejected color
    - `grep "useSearchParams" apps/admin/src/components/content/filter-bar.tsx` confirms URL param sync
    - `grep "300\|debounce\|Deferred" apps/admin/src/components/content/filter-bar.tsx` confirms debounce implementation
    - `grep "router.replace" apps/admin/src/components/content/filter-bar.tsx` confirms URL update
    - `grep "ChevronLeft\|ChevronRight" apps/admin/src/components/ui/pagination-bar.tsx` confirms nav icons
    - `grep "bg-primary" apps/admin/src/components/ui/pagination-bar.tsx` confirms active page style
    - `test -f apps/admin/src/__tests__/status-badge.test.tsx` confirms test exists
    - `grep "bg-amber-100" apps/admin/src/__tests__/status-badge.test.tsx` confirms color assertion
  </acceptance_criteria>
  <done>StatusBadge renders 3 states with correct colors, FilterBar syncs URL params with 300ms debounce, PaginationBar renders numbered pages, status-badge test passes</done>
</task>

</tasks>

<verification>
- `cd apps/admin && pnpm lint` passes
- `cd apps/admin && pnpm test` passes (status-badge test green)
- All 3 i18n files have matching key structure (nav, filter, status, table, empty, error sections)
- Sidebar renders 5 items with lucide icons
- Layout is flex row: sidebar (240px) + main content
</verification>

<success_criteria>
- shadcn Table component installed at src/components/ui/table.tsx
- Sidebar with 5 nav items, active route highlighted, 240px width
- Admin layout modified to sidebar + main two-column
- StatusBadge with 3 color states matching UI-SPEC
- FilterBar with search (debounced), JLPT, category, status dropdowns updating URL
- PaginationBar with numbered pages, cherry pink active state
- All Phase 2 i18n strings in ja/ko/en per UI-SPEC Copywriting Contract
- status-badge.test.tsx passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-list-views/02-02-SUMMARY.md`
</output>
