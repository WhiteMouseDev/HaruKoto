---
phase: 07-i18n-completion-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/admin/src/app/(admin)/vocabulary/page.tsx
  - apps/admin/src/app/(admin)/grammar/page.tsx
  - apps/admin/src/app/(admin)/quiz/page.tsx
  - apps/admin/src/app/(admin)/conversation/page.tsx
  - apps/admin/src/app/(admin)/vocabulary/[id]/page.tsx
  - apps/admin/src/app/(admin)/grammar/[id]/page.tsx
  - apps/admin/src/app/(admin)/quiz/[id]/page.tsx
  - apps/admin/src/app/(admin)/conversation/[id]/page.tsx
  - apps/admin/src/components/content/audit-timeline.tsx
  - apps/admin/src/components/content/content-table.tsx
  - apps/admin/src/components/content/reject-reason-dialog.tsx
autonomous: true
requirements:
  - I18N-04
  - I18N-05
must_haves:
  truths:
    - "No hardcoded Japanese/Korean strings remain in .tsx source files (outside allowlist)"
    - "Switching locale displays all table headers, toasts, errors, placeholders in the selected language"
    - "Category labels in conversation page render via t('category.VALUE') not hardcoded Japanese"
    - "Audit timeline relative time uses next-intl ICU format, not hardcoded Japanese"
    - "Zod validation errors display in the current locale"
  artifacts:
    - path: "apps/admin/src/app/(admin)/vocabulary/page.tsx"
      provides: "Vocabulary list with translated column headers"
      contains: "useTranslations"
    - path: "apps/admin/src/app/(admin)/conversation/page.tsx"
      provides: "Conversation list with translated headers and category labels via t('category.')"
      contains: "t('category."
    - path: "apps/admin/src/components/content/audit-timeline.tsx"
      provides: "Relative time via useTranslations('time') ICU format"
      contains: "useTranslations('time')"
    - path: "apps/admin/src/components/content/content-table.tsx"
      provides: "Translated aria-labels for checkboxes"
      contains: "t('table.selectAll')"
    - path: "apps/admin/src/components/content/reject-reason-dialog.tsx"
      provides: "Cancel button using i18n key instead of hardcoded キャンセル"
      contains: "t('review.cancel')"
  key_links:
    - from: "apps/admin/src/app/(admin)/vocabulary/page.tsx"
      to: "apps/admin/messages/*.json"
      via: "useTranslations('table') for column headers"
      pattern: "tCol\\('col\\."
    - from: "apps/admin/src/components/content/audit-timeline.tsx"
      to: "apps/admin/messages/*.json"
      via: "useTranslations('time') for relative time"
      pattern: "tTime\\('(justNow|minutesAgo|hoursAgo|daysAgo)"
---

<objective>
Replace all hardcoded Japanese/Korean strings in source .tsx files with i18n `t()` calls.

Purpose: Fulfill I18N-04 (no hardcoded CJK strings) and I18N-05 (locale switching works for all text). This covers 8 groups of hardcoded strings identified in research: table headers, toast/error messages, Zod errors, placeholders, aria-labels, cancel button, audit timeline, and category labels.

Output: All 11 source files updated to use `useTranslations()` calls instead of hardcoded strings. The hardcoded-strings validation test (from Plan 01) should pass after this plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-i18n-completion-accessibility/07-CONTEXT.md
@.planning/phases/07-i18n-completion-accessibility/07-RESEARCH.md
@.planning/phases/07-i18n-completion-accessibility/07-01-SUMMARY.md

@apps/admin/messages/ko.json

<interfaces>
<!-- Column type from content-table.tsx -->
export type Column<T> = {
  key: string;
  header: string;     // <-- This is where hardcoded Japanese strings are passed
  width: string;
  render?: (item: T) => React.ReactNode;
  sortKey?: string;
};

<!-- useTranslations pattern (already used throughout codebase) -->
const t = useTranslations('namespace');
// t('key') returns translated string
// t('key', { param: value }) for interpolation
// t('key', { n: count }) for ICU plural

<!-- Zod schema hook pattern (from RESEARCH.md) — needed for detail pages -->
function useMySchema() {
  const t = useTranslations('validation');
  return useMemo(() =>
    z.object({ field: z.string().refine(fn, { message: t('invalidJsonArray') }) }),
    [t]
  );
}

<!-- AuditTimeline uses useTranslations('audit') already -->
<!-- FilterBar uses useTranslations('filter') already -->
<!-- ContentTable uses useTranslations('table') already -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded strings in list pages and shared components</name>
  <files>
    apps/admin/src/app/(admin)/vocabulary/page.tsx,
    apps/admin/src/app/(admin)/grammar/page.tsx,
    apps/admin/src/app/(admin)/quiz/page.tsx,
    apps/admin/src/app/(admin)/conversation/page.tsx,
    apps/admin/src/components/content/audit-timeline.tsx,
    apps/admin/src/components/content/content-table.tsx,
    apps/admin/src/components/content/reject-reason-dialog.tsx
  </files>
  <read_first>
    apps/admin/src/app/(admin)/vocabulary/page.tsx
    apps/admin/src/app/(admin)/grammar/page.tsx
    apps/admin/src/app/(admin)/quiz/page.tsx
    apps/admin/src/app/(admin)/conversation/page.tsx
    apps/admin/src/components/content/audit-timeline.tsx
    apps/admin/src/components/content/content-table.tsx
    apps/admin/src/components/content/reject-reason-dialog.tsx
    apps/admin/messages/ko.json
  </read_first>
  <action>
**A. Four list pages — replace hardcoded column headers:**

In each list page component function, add `const tCol = useTranslations('table');` (or reuse existing `useTranslations('table')` if present). Replace every hardcoded Japanese header string in the `columns` array:

`vocabulary/page.tsx`:
- `'単語'` → `tCol('col.word')`
- `'読み方'` → `tCol('col.reading')`
- `'意味'` → `tCol('col.meaningKo')`
- `'ステータス'` → `tCol('col.status')`
- `'更新日'` → `tCol('col.updatedAt')`

`grammar/page.tsx`:
- `'パターン'` → `tCol('col.pattern')`
- `'説明'` → `tCol('col.explanation')`
- `'ステータス'` → `tCol('col.status')`
- `'更新日'` → `tCol('col.updatedAt')`

`quiz/page.tsx`:
- `'問題文'` → `tCol('col.sentence')`
- `'種類'` → `tCol('col.quizType')`
- `'ステータス'` → `tCol('col.status')`
- `'更新日'` → `tCol('col.updatedAt')`

`conversation/page.tsx`:
- `'タイトル'` → `tCol('col.title')`
- `'カテゴリ'` → `tCol('col.category')`
- `'ステータス'` → `tCol('col.status')`
- `'更新日'` → `tCol('col.updatedAt')`

**B. `conversation/page.tsx` — translate SCENARIO_CATEGORIES (per D-01):**

Add `const tCat = useTranslations('category');` to the component.

Replace the hardcoded `SCENARIO_CATEGORIES` array's labels:
```typescript
// Before:
const SCENARIO_CATEGORIES = [
  { value: 'TRAVEL', label: '旅行' },
  ...
];

// After: Build dynamically using tCat()
const SCENARIO_CATEGORIES = [
  'TRAVEL', 'SHOPPING', 'RESTAURANT', 'BUSINESS',
  'DAILY_LIFE', 'EMERGENCY', 'TRANSPORTATION', 'HEALTHCARE',
].map((key) => ({ value: key, label: tCat(key) }));
```

Also update the column `render` for category — wherever it does `cat?.label ?? item.category`, it should use `tCat(item.category)` as the fallback since category values are the enum keys.

**C. `audit-timeline.tsx` — replace formatRelativeTime with i18n (per D-02):**

Add `const tTime = useTranslations('time');` to the `AuditTimeline` component body.

Replace the `formatRelativeTime` function. Move the logic INSIDE the component (since it needs `tTime`):

```typescript
// Inside AuditTimeline component:
const tTime = useTranslations('time');

function formatRelativeTime(dateStr: string): string {
  const diffMs = Date.now() - new Date(dateStr).getTime();
  const diffMin = Math.floor(diffMs / 60_000);
  if (diffMin < 1) return tTime('justNow');
  if (diffMin < 60) return tTime('minutesAgo', { n: diffMin });
  const diffH = Math.floor(diffMin / 60);
  if (diffH < 24) return tTime('hoursAgo', { n: diffH });
  return tTime('daysAgo', { n: Math.floor(diffH / 24) });
}
```

Remove the module-level `formatRelativeTime` function entirely.

**D. `content-table.tsx` — replace hardcoded aria-labels:**

The component already has `useTranslations('table')`. Replace:
- `aria-label="全て選択"` → `aria-label={t('selectAll')}`
- `` aria-label={`行 ${item.id} を選択`} `` → `aria-label={t('selectRow', { id: item.id })}`

**E. `reject-reason-dialog.tsx` — replace キャンセル:**

The component already has `const t = useTranslations('review');`. Replace:
- `キャンセル` → `{t('cancel')}`
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese/apps/admin && grep -rn '単語\|読み方\|意味\|ステータス\|更新日\|パターン\|説明\|問題文\|種類\|タイトル\|カテゴリ\|キャンセル\|たった今\|分前\|時間前\|日前\|全て選択\|を選択' src/app/ src/components/content/ --include='*.tsx' | grep -v '__tests__' | grep -v 'node_modules' | head -20 ; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `vocabulary/page.tsx` contains `tCol('col.word')` and does NOT contain `'単語'` as a column header string
    - `grammar/page.tsx` contains `tCol('col.pattern')` and does NOT contain `'パターン'` as a column header string
    - `quiz/page.tsx` contains `tCol('col.sentence')` and does NOT contain `'問題文'` as a column header string
    - `conversation/page.tsx` contains `tCat(` calls for category labels and does NOT contain `'旅行'` or `'ショッピング'`
    - `audit-timeline.tsx` contains `useTranslations('time')` and does NOT contain `'たった今'` or `分前`
    - `content-table.tsx` does NOT contain `全て選択` or `を選択`
    - `reject-reason-dialog.tsx` does NOT contain `キャンセル`
    - No grep matches for hardcoded Japanese column headers in src/app/ or src/components/content/ .tsx files
  </acceptance_criteria>
  <done>All 7 shared/list files have zero hardcoded CJK strings, using i18n t() calls instead</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded strings in detail/edit pages (Zod, toast, placeholders)</name>
  <files>
    apps/admin/src/app/(admin)/vocabulary/[id]/page.tsx,
    apps/admin/src/app/(admin)/grammar/[id]/page.tsx,
    apps/admin/src/app/(admin)/quiz/[id]/page.tsx,
    apps/admin/src/app/(admin)/conversation/[id]/page.tsx
  </files>
  <read_first>
    apps/admin/src/app/(admin)/vocabulary/[id]/page.tsx
    apps/admin/src/app/(admin)/grammar/[id]/page.tsx
    apps/admin/src/app/(admin)/quiz/[id]/page.tsx
    apps/admin/src/app/(admin)/conversation/[id]/page.tsx
    apps/admin/messages/ko.json
  </read_first>
  <action>
All four detail pages share similar patterns. Apply these changes to each:

**A. Toast "no changes" message — all 4 pages:**

Find `toast.info('変更がありません')` (or similar) and replace with:
```typescript
const tEdit = useTranslations('edit');
// ...
toast.info(tEdit('noChanges'));
```

Add `useTranslations('edit')` to the component if not already present. If the component already uses `useTranslations('edit')` under a different variable name, reuse that.

**B. Error display "データの読み込みに失敗しました" — all 4 pages:**

Find inline JSX like `<div>データの読み込みに失敗しました</div>` or `<p>データの読み込みに失敗しました</p>` and replace with:
```typescript
const tError = useTranslations('error');
// ...
<div>{tError('failedToLoad')}</div>
```

The `error.failedToLoad` key already exists in all 3 locale files.

**C. Zod schema with Japanese error message — grammar/[id], quiz/[id], conversation/[id]:**

These pages have module-level Zod schemas containing `.refine(fn, { message: '有効なJSON配列を入力してください' })`.

Move the schema inside the component using a hook pattern:

```typescript
import { useMemo } from 'react';

// Inside the component:
const tVal = useTranslations('validation');

const schema = useMemo(() =>
  z.object({
    // ... keep all existing fields ...
    exampleSentences: z.string().optional().refine(
      (val) => { /* keep existing validation logic unchanged */ },
      { message: tVal('invalidJsonArray') }
    ),
    // ... other fields with .refine() get same treatment ...
  }),
  [tVal]
);
```

Ensure `zodResolver(schema)` in the `useForm()` call references the new local `schema` variable (not the old module-level one). Remove the old module-level schema const.

**D. Placeholder strings — quiz/[id] and conversation/[id]:**

`quiz/[id]/page.tsx`:
- ClozeForm: Replace `placeholder='["選択肢1","選択肢2"]'` with `placeholder={tEdit('placeholder.options')}`
- SentenceArrangeForm: Replace `placeholder='["トークン1","トークン2"]'` with `placeholder={tEdit('placeholder.tokens')}`

`conversation/[id]/page.tsx`:
- Replace `placeholder='["表現1","表現2"]'` with `placeholder={tEdit('placeholder.keyExpressions')}`

Ensure `tEdit = useTranslations('edit')` is available in the component scope where these placeholders are used.

**E. Run the hardcoded string detection test to verify completeness:**

After all changes, the test from Plan 01 (`hardcoded-strings.test.ts`) should pass with zero violations.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese/apps/admin && npx vitest run src/__tests__/hardcoded-strings.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `vocabulary/[id]/page.tsx` does NOT contain `変更がありません` or `データの読み込みに失敗しました`
    - `grammar/[id]/page.tsx` does NOT contain `変更がありません` or `有効なJSON配列`
    - `quiz/[id]/page.tsx` does NOT contain `変更がありません`, `有効なJSON配列`, `選択肢1`, or `トークン1`
    - `conversation/[id]/page.tsx` does NOT contain `変更がありません`, `有効なJSON配列`, or `表現1`
    - All 4 detail pages contain `useTranslations('edit')` or equivalent
    - grammar/[id], quiz/[id], conversation/[id] contain `useTranslations('validation')` for Zod error
    - Zod schemas are defined inside the component (not at module level) in grammar/[id], quiz/[id], conversation/[id]
    - `npx vitest run src/__tests__/hardcoded-strings.test.ts` exits 0 (all CJK strings eliminated)
  </acceptance_criteria>
  <done>All 4 detail pages use i18n for toasts, errors, Zod messages, and placeholders. Hardcoded string detection test passes.</done>
</task>

</tasks>

<verification>
- `cd apps/admin && npx vitest run src/__tests__/hardcoded-strings.test.ts` passes (zero CJK violations)
- `cd apps/admin && npx vitest run src/__tests__/locale-key-parity.test.ts` still passes
- grep for any remaining hardcoded Japanese strings in src/ returns zero results (excluding allowlisted files)
- `pnpm lint` passes in apps/admin
</verification>

<success_criteria>
- Zero hardcoded CJK strings remain in .tsx source files (confirmed by hardcoded-strings test)
- All table headers, toast messages, error messages, Zod errors, placeholders, aria-labels, cancel button, relative time, and category labels use t() calls
- Locale key parity test still passes
- TypeScript compilation succeeds (no type errors from schema refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/07-i18n-completion-accessibility/07-02-SUMMARY.md`
</output>
