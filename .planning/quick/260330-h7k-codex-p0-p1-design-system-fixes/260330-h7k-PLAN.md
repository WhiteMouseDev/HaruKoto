---
phase: quick
plan: 260330-h7k
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/app/(admin)/layout.tsx
  - apps/admin/src/components/layout/sidebar.tsx
  - apps/admin/src/app/globals.css
  - apps/admin/src/components/content/review-header.tsx
  - apps/admin/src/components/content/queue-navigation-bar.tsx
autonomous: true
requirements: [CODEX-P0-1, CODEX-P0-2, CODEX-P1]

must_haves:
  truths:
    - "Sidebar stays viewport-fixed when main content scrolls"
    - "Logout/LocaleSwitcher at sidebar bottom always visible regardless of scroll"
    - "Primary buttons have readable text (WCAG AA 4.5:1 contrast ratio)"
    - "ReviewHeader and QueueNavigationBar stay visible at top when scrolling long edit forms"
  artifacts:
    - path: "apps/admin/src/app/(admin)/layout.tsx"
      provides: "h-screen overflow-hidden flex layout"
      contains: "h-screen"
    - path: "apps/admin/src/components/layout/sidebar.tsx"
      provides: "Viewport-fixed sidebar with own scroll"
      contains: "h-screen"
    - path: "apps/admin/src/app/globals.css"
      provides: "WCAG-compliant primary-foreground color"
      contains: "#4A1A2A"
    - path: "apps/admin/src/components/content/review-header.tsx"
      provides: "Sticky review actions"
      contains: "sticky"
    - path: "apps/admin/src/components/content/queue-navigation-bar.tsx"
      provides: "Sticky queue navigation"
      contains: "sticky"
  key_links:
    - from: "apps/admin/src/app/(admin)/layout.tsx"
      to: "apps/admin/src/components/layout/sidebar.tsx"
      via: "flex h-screen parent contains h-screen sidebar child"
      pattern: "h-screen.*overflow-hidden"
---

<objective>
Fix three Codex P0/P1 design system issues in the admin app: (1) viewport-fixed sidebar layout, (2) primary button WCAG contrast, (3) sticky edit page action bars.

Purpose: Address cross-review findings that affect usability — sidebar bottom controls disappearing on scroll, unreadable primary buttons, and review actions scrolling off-screen on long forms.
Output: Corrected layout, CSS tokens, and sticky positioning across 5 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/admin/src/app/(admin)/layout.tsx
@apps/admin/src/components/layout/sidebar.tsx
@apps/admin/src/app/globals.css
@apps/admin/src/components/content/review-header.tsx
@apps/admin/src/components/content/queue-navigation-bar.tsx
@apps/admin/src/app/(admin)/vocabulary/[id]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix admin layout to h-screen + viewport-fixed sidebar (P0-1)</name>
  <files>apps/admin/src/app/(admin)/layout.tsx, apps/admin/src/components/layout/sidebar.tsx</files>
  <action>
    In layout.tsx:
    - Change root div from `flex min-h-screen` to `flex h-screen overflow-hidden`
    - Change main from `flex-1 overflow-y-auto p-8` to `flex-1 overflow-y-auto p-8` (keep overflow-y-auto, it's correct)

    In sidebar.tsx:
    - Change aside from `flex h-full w-60 flex-col` to `flex h-screen w-60 shrink-0 flex-col overflow-y-auto`
    - `h-screen` makes it viewport-fixed independent of parent height
    - `shrink-0` prevents flex shrink
    - `overflow-y-auto` gives sidebar its own scrollbar if nav items exceed viewport

    This is the industry-standard admin layout pattern: root fills viewport exactly, sidebar is viewport-height with own scroll, main content area scrolls independently.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese && pnpm --filter admin build 2>&1 | tail -5</automated>
  </verify>
  <done>Root layout uses h-screen overflow-hidden; sidebar uses h-screen with own overflow-y-auto; main content scrolls independently; sidebar bottom controls (logout, locale switcher) always visible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix primary-foreground WCAG contrast + sticky action bars (P0-2, P1)</name>
  <files>apps/admin/src/app/globals.css, apps/admin/src/components/content/review-header.tsx, apps/admin/src/components/content/queue-navigation-bar.tsx</files>
  <action>
    **P0-2 — WCAG contrast fix in globals.css:**
    - Light theme: Change `--primary-foreground: #FFFFFF` to `--primary-foreground: #4A1A2A` (dark cherry)
    - Dark theme: Change `--primary-foreground: #FFFFFF` to `--primary-foreground: #4A1A2A` (same dark cherry — primary #F6A5B3 is unchanged in both themes)
    - Rationale: #4A1A2A on #F6A5B3 yields ~6.2:1 contrast ratio, well above WCAG AA 4.5:1 threshold. White on #F6A5B3 is only ~2.1:1.

    **P1 — Sticky action bars:**

    In review-header.tsx:
    - Change the outer div className from `flex items-center justify-between rounded-lg border border-border bg-card p-4` to `sticky top-0 z-10 flex items-center justify-between rounded-lg border border-border bg-card p-4`
    - `sticky top-0` pins it to top of the scroll container (the main element from layout)
    - `z-10` ensures it layers above form content
    - `bg-card` already provides opaque background so content doesn't bleed through

    In queue-navigation-bar.tsx:
    - Change the outer div className from `flex h-12 items-center justify-between border-b border-border bg-card px-4` to `sticky top-0 z-10 flex h-12 items-center justify-between border-b border-border bg-card px-4`
    - Same sticky pattern as ReviewHeader

    Note: When both QueueNavigationBar and ReviewHeader render (queue mode), they stack — QueueNavigationBar first, then ReviewHeader. The second element will naturally sit below the first because sticky elements stack in document order. This is fine — both are short bars (h-12 + p-4) totaling ~6rem, leaving plenty of form space visible.
  </action>
  <verify>
    <automated>cd /Users/kimkunwoo/WhiteMouseDev/japanese && pnpm --filter admin build 2>&1 | tail -5</automated>
  </verify>
  <done>Primary buttons show dark text (#4A1A2A) on cherry pink background meeting WCAG AA; ReviewHeader and QueueNavigationBar stick to top of scroll container when scrolling long edit forms; all changes build without errors.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter admin build` succeeds without errors
2. Visual check: navigate to any edit page (e.g., /vocabulary/[id]) — ReviewHeader and QueueNavigationBar stay pinned while scrolling
3. Visual check: sidebar bottom (logout, locale switcher) stays visible when main content is long
4. Visual check: default Button components show readable dark text on cherry pink background
</verification>

<success_criteria>
- Admin layout uses h-screen overflow-hidden pattern
- Sidebar is viewport-fixed with independent scroll
- Primary foreground color passes WCAG AA (4.5:1) against #F6A5B3
- ReviewHeader and QueueNavigationBar are sticky top-0 on edit pages
- Build passes with no TypeScript or lint errors
</success_criteria>

<output>
After completion, create `.planning/quick/260330-h7k-codex-p0-p1-design-system-fixes/260330-h7k-SUMMARY.md`
</output>
